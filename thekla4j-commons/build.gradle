plugins {
    id 'com.teststeps.java-conventions'
    id 'com.teststeps.thekla4j.publish'
}

description = 'commonly used files in library'

publishing {
    publications {
        pluginMaven(MavenPublication) {
            artifactId = 'thekla4j-commons'
        }
    }
}